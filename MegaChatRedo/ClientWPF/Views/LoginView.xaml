<UserControl x:Class="ClientWPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:ClientWPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel}">
    <StackPanel Margin="10" Width="100" VerticalAlignment="Center">
        <TextBlock Text="Your nickname: " Margin="0,0,0,10"></TextBlock>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding DoLogin}"/>
            </TextBox.InputBindings>
        </TextBox>
        <Button Content="GO!" Command="{Binding DoLogin}"></Button>
    </StackPanel>
</UserControl>
